# ch25 - Presentation: Cutscenes and Attract Mode

Chapter 25 of *Building Two Classic Platform Games*.

Open `ch25.slnx` for this chapter alone, or `../PlatformBook.slnx` for all
forty-six, foldered by the book's four parts.

    Both      dotnet build ch25.slnx -c Release
    Android   dotnet build Android/ch25.Android.csproj -c Release -t:Run
    iOS       dotnet build iOS/ch25.iOS.csproj -c Release \
                  -p:RuntimeIdentifier=ios-arm64

Orientation: Portrait (locked)
Shared code in `Game/`, art and audio linked from `../../assets/climber`.
