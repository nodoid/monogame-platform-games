# ch12 - Persisting High Scores

Chapter 12 of *Building Two Classic Platform Games*.

Open `ch12.slnx` for this chapter alone, or `../PlatformBook.slnx` for all
forty-six, foldered by the book's four parts.

    Both      dotnet build ch12.slnx -c Release
    Android   dotnet build Android/ch12.Android.csproj -c Release -t:Run
    iOS       dotnet build iOS/ch12.iOS.csproj -c Release \
                  -p:RuntimeIdentifier=ios-arm64

Orientation: Portrait (locked)
Shared code in `Game/`, art and audio linked from `../../assets/climber`.
