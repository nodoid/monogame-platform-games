# ch18 - Enemy Variety Across Stages

Chapter 18 of *Building Two Classic Platform Games*.

Open `ch18.slnx` for this chapter alone, or `../PlatformBook.slnx` for all
forty-six, foldered by the book's four parts.

    Both      dotnet build ch18.slnx -c Release
    Android   dotnet build Android/ch18.Android.csproj -c Release -t:Run
    iOS       dotnet build iOS/ch18.iOS.csproj -c Release \
                  -p:RuntimeIdentifier=ios-arm64

Orientation: Portrait (locked)
Shared code in `Game/`, art and audio linked from `../../assets/climber`.
