# ch28 - Flick-Screen versus Scrolling

Chapter 28 of *Building Two Classic Platform Games*.

Open `ch28.slnx` for this chapter alone, or `../PlatformBook.slnx` for all
forty-six, foldered by the book's four parts.

    Both      dotnet build ch28.slnx -c Release
    Android   dotnet build Android/ch28.Android.csproj -c Release -t:Run
    iOS       dotnet build iOS/ch28.iOS.csproj -c Release \
                  -p:RuntimeIdentifier=ios-arm64

Orientation: Landscape (locked)
Shared code in `Game/`, art and audio linked from `../../assets/hunch`.
