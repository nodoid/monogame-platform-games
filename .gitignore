# Build output
bin/
obj/
*.user
*.binlog

# Generator scratch (gen_audio.py's default target when run without a path)
/out/

# macOS
.DS_Store

# The book itself is not in source control: not the chapter sources, not the
# generated chapter documents, not the captured figures, not the covers, and
# not the publisher's author template. All of it is reproducible from tools/
# (gen_book.py, capture.py, gen_covers.py) against the code that is tracked.
/book/
/Chapters.odt
